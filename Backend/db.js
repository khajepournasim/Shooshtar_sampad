const db=require('better-sqlite3')(process.env.DB_FILE||'school.db');db.pragma('journal_mode=WAL');db.exec(`
CREATE TABLE IF NOT EXISTS students(id INTEGER PRIMARY KEY AUTOINCREMENT,national_id TEXT UNIQUE NOT NULL,password_hash TEXT NOT NULL,first_name TEXT NOT NULL,last_name TEXT NOT NULL,grade TEXT NOT NULL,field TEXT,class_name TEXT,photo TEXT DEFAULT '');
CREATE TABLE IF NOT EXISTS classes(id INTEGER PRIMARY KEY AUTOINCREMENT,title TEXT NOT NULL,teacher TEXT,weekday TEXT,time TEXT,online_url TEXT DEFAULT '');
CREATE TABLE IF NOT EXISTS enrollments(student_id INTEGER NOT NULL,class_id INTEGER NOT NULL,PRIMARY KEY(student_id,class_id));
CREATE TABLE IF NOT EXISTS grades(id INTEGER PRIMARY KEY AUTOINCREMENT,student_id INTEGER NOT NULL,subject TEXT NOT NULL,score REAL NOT NULL,term TEXT);
CREATE TABLE IF NOT EXISTS consents(id INTEGER PRIMARY KEY AUTOINCREMENT,student_id INTEGER NOT NULL,title TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',updated_at TEXT);
CREATE TABLE IF NOT EXISTS announcements(id INTEGER PRIMARY KEY AUTOINCREMENT,title TEXT NOT NULL,body TEXT,audience TEXT DEFAULT 'all',created_at TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS exams(id INTEGER PRIMARY KEY AUTOINCREMENT,student_id INTEGER NOT NULL,subject TEXT NOT NULL,exam_date TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS files(id INTEGER PRIMARY KEY AUTOINCREMENT,student_id INTEGER,title TEXT NOT NULL,url TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS feedback(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,subject TEXT NOT NULL,message TEXT NOT NULL,created_at TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS occasions(id INTEGER PRIMARY KEY AUTOINCREMENT,title TEXT NOT NULL,date TEXT NOT NULL);
`);module.exports=db;