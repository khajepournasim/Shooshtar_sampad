const faDigits=s=>String(s??'').replace(/\d/g,d=>'۰۱۲۳۴۵۶۷۸۹'[d]);
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.js-date').forEach(x=>x.textContent=new Date().toLocaleDateString('fa-IR'))});
function openImage(src,alt='تصویر'){const m=document.getElementById('imageModal');if(!m)return;document.getElementById('modalImage').src=src;document.getElementById('modalImage').alt=alt;m.classList.add('open')}
function closeImage(){document.getElementById('imageModal')?.classList.remove('open')}
function sendFeedback(e){e.preventDefault();const f=e.currentTarget;const body={name:f.name.value,subject:f.subject.value,message:f.message.value};fetch('/api/feedback',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}).then(r=>{if(!r.ok)throw 0;return r.json()}).then(()=>{f.reset();alert('پیام شما با موفقیت ثبت شد.')}).catch(()=>alert('پیام در سامانه ثبت نشد. لطفاً بعداً دوباره تلاش کنید یا از طریق ایتا با مدرسه در ارتباط باشید.'))}
async function loadOccasions(){const box=document.getElementById('occasionList');if(!box)return;try{const r=await fetch('/api/occasions/today');const d=await r.json();box.innerHTML=d.items?.length?d.items.map(x=>`<div class="occasion-item"><span class="occasion-dot"></span><span>${x.title}</span></div>`).join(''):'<div class="occasion-item">امروز مناسبت ثبت‌شده‌ای در تقویم مدرسه وجود ندارد.</div>';const status=document.getElementById('todayStatus');if(status)status.textContent=d.items?.length?`${faDigits(d.items.length)} مناسبت`:'روز عادی';}catch{box.innerHTML='<div class="occasion-item">تقویم مدرسه در دسترس نیست.</div>'}}
function logout(){localStorage.removeItem('studentToken');localStorage.removeItem('studentSession');location.href='student-login.html'}
loadOccasions();
